rules_version = '2';
 
service cloud.firestore {
  match /databases/{database}/documents {
    // By default, deny all reads and writes
    // This is a good starting point to prevent unauthorized access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
